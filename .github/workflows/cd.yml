name: CD

on:
  release:
    types:
      - prereleased
      - released

jobs:
  milestone:
    if: startsWith(github.ref, 'refs/tags')
    uses: ComPWA/actions/.github/workflows/close-milestone.yml@dev
  pypi:
    if: startsWith(github.ref, 'refs/tags')
    uses: ComPWA/actions/.github/workflows/publish-to-pypi.yml@dev
  push:
    if: startsWith(github.ref, 'refs/tags') && !github.event.release.prerelease
    uses: ComPWA/actions/.github/workflows/push-to-version-branches.yml@dev
