name: ampform
channels:
  - defaults
dependencies:
  - python==3.11.*
  - pip
  - graphviz
  - pip:
      - -c .constraints/py3.11.txt -e .[dev]
variables:
  PRETTIER_LEGACY_CLI: 1
  PYDEVD_DISABLE_FILE_VALIDATION: 1
  PYTHONHASHSEED: 0
